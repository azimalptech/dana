<?xml version="1.0" encoding="utf-8"?>
<!-- Same as drawable/launch_background.xml — brand maroon rather than
     ?android:colorBackground, which resolved to black in dark mode and
     white in light, neither of which is the splash design. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/dana_brand" />
</layer-list>
